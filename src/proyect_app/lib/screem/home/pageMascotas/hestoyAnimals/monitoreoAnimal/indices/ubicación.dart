import 'package:flutter/material.dart';

class ubicacionPet extends StatelessWidget {
  const ubicacionPet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ubicacion"),
      ),
    );
  }
}